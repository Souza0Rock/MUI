import * as M from "@mui/material";

function ButtonSubmit () {
  return (
    <M.Button variant="contained" color="primary" sx={{width: '100%'}} >
      enviar
    </M.Button>
  )
}

export default ButtonSubmit